// document.ready
document.addEventListener("DOMContentLoaded", function(event) { 

    const player = new Plyr(
        '#player', 
        {
            title: 'Example Title'
        }
    );

});
